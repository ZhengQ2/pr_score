{
  "name": "pr-score",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173 --bind 127.0.0.1 --directory dist",
    "test": "node --test tests/*.test.js",
    "check": "node --check dist/app.js && node --check dist/express-entry.js && node --check dist/systems.js && node --check dist/language-tests.js && node --check dist/occupations.js"
  }
}
